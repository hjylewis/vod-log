{
  "name": "vod-log-worker",
  "version": "1.0.0",
  "description": "worker that pulls from twitch and riot apis to at vods to database",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henry Lewis",
  "license": "ISC",
  "dependencies": {
    "firebase": "^3.2.0",
  }
}
